{
  "buildCommand": "cd ../.. && pnpm turbo run build",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ],
  "functions": {
    "../../api/index.ts": {
      "memory": 1024,
      "maxDuration": 300
    }
  }
}

