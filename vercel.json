{
  "version": 2,
  "buildCommand": "pnpm install && pnpm --filter @genie/shared build && pnpm --filter @genie/crawler build && pnpm --filter @genie/analyzer build && pnpm --filter @genie/generator build && pnpm --filter @genie/ai-services build && cd apps/api && pnpm prisma:generate && cd ../web && pnpm build",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "outputDirectory": "apps/web/.next",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ],
  "functions": {
    "api/index.ts": {
      "memory": 1024,
      "maxDuration": 300
    }
  },
  "env": {
    "NODE_ENV": "production"
  }
}

